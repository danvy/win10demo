<Page
    x:Class="Win10Demo.SysInfoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Win10Demo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<Pivot>
			<PivotItem Header="Battery">
				<StackPanel>
					<TextBlock Text="Battery" />
					<TextBlock x:Name="BatteryStatusText" />
					<TextBlock x:Name="BatteryChargeRateText" />
					<TextBlock x:Name="BatteryDesignCapacityText" />
					<TextBlock x:Name="BatteryFullChargeCapacityText" />
					<TextBlock x:Name="BatteryRemainingCapacityText" />
					<TextBlock x:Name="BatteryLevelText" />
				</StackPanel>
			</PivotItem>
			<PivotItem Header="Memory">
				<StackPanel>
					<TextBlock Text="Memory Manager" />
					<TextBlock x:Name="ReportText" />
				</StackPanel>
			</PivotItem>
		</Pivot>
	</Grid>
</Page>
